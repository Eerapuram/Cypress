{
  "name": "cypress-9",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run",
    "clean:reports": "if exist output rmdir /s/q output",
    "merge": "mochawesome-merge cypress/reports/*.json > cypress/reports/output,json",
    "generate": "marge cypress/reports/output.json --reportDir ./ --inline"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.5.0",
    "drag-drop": "^7.2.0",
    "iframe": "^1.0.0"
  },
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.2.1",
    "mocha": "^10.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.1"
  }
}
